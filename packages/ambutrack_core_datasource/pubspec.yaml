name: ambutrack_core_datasource
description: Core datasources package for AmbuTrack - Contains entities, models, and datasource implementations
publish_to: 'none'

version: 1.0.0

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.8.3

  # Utilities
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

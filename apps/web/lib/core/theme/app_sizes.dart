/// Constantes de tamaños y espaciados de la aplicación AmbuTrack
///
/// Define todos los valores de padding, margin, border radius, tamaños de fuente, etc.
/// para mantener consistencia visual y evitar magic numbers.
class AppSizes {
  AppSizes._();

  // === PADDING Y SPACING ===

  /// Espaciado extra pequeño (4px)
  static const double spacingXs = 4.0;

  /// Espaciado pequeño (8px)
  static const double spacingSmall = 8.0;

  /// Espaciado mediano (12px)
  static const double spacingMedium = 12.0;

  /// Espaciado normal (16px)
  static const double spacing = 16.0;

  /// Espaciado grande (20px)
  static const double spacingLarge = 20.0;

  /// Espaciado extra grande (24px)
  static const double spacingXl = 24.0;

  /// Espaciado extra extra grande (32px)
  static const double spacingXxl = 32.0;

  /// Espaciado masivo (48px)
  static const double spacingMassive = 48.0;

  /// Padding pequeño (8px)
  static const double paddingSmall = 8.0;

  /// Padding mediano (12px)
  static const double paddingMedium = 12.0;

  /// Padding normal (16px)
  static const double padding = 16.0;

  /// Padding grande (20px)
  static const double paddingLarge = 20.0;

  /// Padding extra grande (24px)
  static const double paddingXl = 24.0;

  // === BORDER RADIUS ===

  /// Radio de borde pequeño (8px)
  static const double radiusSmall = 8.0;

  /// Radio de borde mediano (10px)
  static const double radiusMedium = 10.0;

  /// Radio de borde normal (12px)
  static const double radius = 12.0;

  /// Radio de borde grande (16px)
  static const double radiusLarge = 16.0;

  /// Radio de borde extra grande (20px)
  static const double radiusXl = 20.0;

  // === TAMAÑOS DE ICONOS ===

  /// Tamaño de icono pequeño (16px)
  static const double iconSmall = 16.0;

  /// Tamaño de icono mediano (18px)
  static const double iconMedium = 18.0;

  /// Tamaño de icono normal (24px)
  static const double icon = 24.0;

  /// Tamaño de icono grande (28px)
  static const double iconLarge = 28.0;

  /// Tamaño de icono extra grande (40px)
  static const double iconXl = 40.0;

  /// Tamaño de icono masivo (48px)
  static const double iconMassive = 48.0;

  /// Tamaño de icono gigante (64px)
  static const double iconHuge = 64.0;

  // === TAMAÑOS DE FUENTE ===

  /// Tamaño de fuente extra pequeño (12px)
  static const double fontXs = 12.0;

  /// Tamaño de fuente pequeño (14px)
  static const double fontSmall = 14.0;

  /// Tamaño de fuente normal (16px)
  static const double font = 16.0;

  /// Tamaño de fuente mediano (18px)
  static const double fontMedium = 18.0;

  /// Tamaño de fuente grande (20px)
  static const double fontLarge = 20.0;

  /// Tamaño de fuente título (28px)
  static const double fontTitle = 28.0;

  /// Tamaño de fuente extra grande (32px)
  static const double fontXl = 32.0;

  // === ELEVACIÓN Y SOMBRAS ===

  /// Desenfoque de sombra pequeño (8px)
  static const double shadowSmall = 8.0;

  /// Desenfoque de sombra mediano (12px)
  static const double shadowMedium = 12.0;

  // === DIMENSIONES DE COMPONENTES ===

  /// Ancho de punto de estado (8px)
  static const double statusDot = 8.0;

  /// Ancho de diálogo estándar (800px)
  static const double dialogWidth = 800.0;
}

// Basic Flutter widget test para AmbuTrack Mobile

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Basic app initialization smoke test', (WidgetTester tester) async {
    // Test básico para verificar que la app se inicializa
    // Los tests completos se implementarán en el futuro
    expect(true, isTrue);
  });
}
